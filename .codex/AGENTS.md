# Agent Reminders

- Load `.codex/context/repo.map.yaml` and relevant domain/context files before deep code reads.
- Use project agents in `.codex/agents/` whenever a task matches their scope; say which agent you are using.
- If no agent applies, state briefly why.
